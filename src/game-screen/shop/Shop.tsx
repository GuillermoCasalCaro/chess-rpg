import { Text } from '@mantine/core';

export const Shop = () => {
    return (
        <Text
            size="xl"
            fw={900}
            variant="gradient"
            gradient={{ from: 'blue', to: 'cyan', deg: 90 }}
        >
            Shop
        </Text>
    );
};
